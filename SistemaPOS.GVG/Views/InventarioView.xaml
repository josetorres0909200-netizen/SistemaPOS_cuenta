<UserControl x:Class="SistemaPOS.Desktop.Views.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#fabfb7" Loaded="UserControl_Loaded" Height="382" Width="888">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Width="300" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
            <Button Content="🔍 Buscar" Width="80" Margin="10,0,0,0" Background="#F0F0F0" BorderThickness="0"/>
        </StackPanel>

        <DataGrid x:Name="dgInventario" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" 
                  GridLinesVisibility="Horizontal" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="80"/>
                <DataGridTextColumn Header="Descripción del Producto" Binding="{Binding Descripcion}" Width="*"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="120"/>
                <DataGridTextColumn Header="Acabado" Binding="{Binding Acabado}" Width="100"/>
                <DataGridTextColumn Header="Tamaño" Binding="{Binding Tamanio}" Width="80"/>
                <DataGridTextColumn Header="Costo" Binding="{Binding PrecioCosto, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>